name: resite_ip
channels:
  - conda-forge
  - defaults
dependencies:
  - pip
  - bottleneck
  - dask
  - scipy
  - toolz
  - xarray
  - geopandas
  - pyyaml
  - xlrd
  - geopy
  - shapely
  - pycountry

  - pip:
      - julia