name: resite_ip
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.6
  - pip
  - bottleneck
  - dask
  - scipy
  - toolz
  - xarray
  - geopandas
  - pyyaml
  - xlrd
  - geopy
  - shapely
  - pycountry
  - pyyaml

  - pip:
      - julia
      - windpowerlib